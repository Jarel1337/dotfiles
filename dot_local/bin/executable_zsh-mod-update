#!/bin/zsh

readonly ZMOD_DIR="$XDG_DATA_HOME/zsh"

check_dir_and_pull(){
	if [[ -d "$ZMOD_DIR/$1" ]]; then
		 git -C "$ZMOD_DIR/$1" pull --depth=1
	else
		echo "$ZMOD_DIR/$1" is not a directory, skipping
	fi
}

for i in powerlevel10k fsh test; do
	check_dir_and_pull $i &
done

wait
